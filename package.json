{
  "name": "azure-table-utils",
  "version": "0.0.1",
  "description": "A collection of functions for azure table storage to make your life easier.",
  "main": "dist/index.js",
  "scripts": {
    "deploy": "npm version patch && tsc && npm publish",
    "dev": "nodemon src/dev.ts",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "azure",
    "azure table storage",
    "azure storage table",
    "table",
    "storage",
    "blob"
  ],
  "author": "@vatadepalli",
  "license": "MIT",
  "dependencies": {
    "azure-storage": "^2.10.3",
    "isomorphic-fetch": "^3.0.0"
  },
  "devDependencies": {
    "@types/node": "^14.14.16",
    "nodemon": "^2.0.6",
    "ts-node": "^9.1.1",
    "typescript": "^4.1.3"
  }
}
